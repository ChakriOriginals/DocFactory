[project]
name = "docfactory-evals"
version = "0.1.0"
description = "Golden set + eval harness"
requires-python = ">=3.12"
dependencies = [
    "docfactory-core",
]

[tool.uv.sources]
docfactory-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
